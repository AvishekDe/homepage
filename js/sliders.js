function updateSliderUX(slideAmount) {
	$("#chosenUX").html(slideAmount);
}

function updateSliderAP(slideAmount) {
	$("#chosenAP").html(slideAmount);
}

function updateSliderUS(slideAmount) {
	$("#chosenUS").html(slideAmount);
}